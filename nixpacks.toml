[variables]
NODE_ENV = "production"
DATABASE_PATH = "/data/lingo.db"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "mkdir -p /data && npm run db-migrate && npm start"
